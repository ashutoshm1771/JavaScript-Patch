//var message = '';
//console.log(global.message); // undefined because message is not in global namespace

// Better to use modules than to declare globally, to reduce ambiguity

// var sayHello = function() {
// }

// window.sayHello();

console.log(module);